{
  "version": 2,
  "builds": [
    {
      "src": "source/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "source/build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/source/$1" }
  ]
}
